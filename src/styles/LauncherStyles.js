import { StyleSheet, Dimensions } from "react-native";
import colors from "../utils/colors";
const LauncherStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.white,
    alignItems: "center",
    justifyContent: "center"
  },
  text: {
    color: colors.balck
  }
});

export default LauncherStyles;
